# Ollama LLM runtime for the translation stack.
# Models are stored in /root/.ollama (mount a volume in compose).
FROM ollama/ollama

# Optional: pin a specific tag, e.g. ollama/ollama:0.3.0
# Default: use latest
